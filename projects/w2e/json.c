#include <stdio.h>
#include <sys/file.h>

int
main (int argc, char **argv)
{
    if (argc == 1)
    {
        fprintf (stderr, "No input file.\n");
        return 1;
    }

    FILE *src;


    return 0;
}