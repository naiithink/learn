#include <stdio.h>
#include <stdlib.h>
#include <sys/stat.h>

#include "yep.h"

static int exit_status;

int
main (int argc, char **argv)
{
    running_ok ok = dne;

    if ()
    execv ()

    return exit_status = ok ? EXIT_SUCCESS : EXIT_FAILURE;
}