# mamba
```
RUN     git clone https://github.com/PyMamba/mamba-framework.git $SRC_DIR \
        && python3 -m venv --prompt='(mamba-dev-pypy) ' mamba-dev-pypy \
        && source mamba-dev-pypy/bin/activate
```