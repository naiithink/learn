#include <stdio.h>
#include <stdlib.h>

int main(void)
{

    printf("%li\n", sizeof(double));
    // char in[2*sizeof(float)];
    // float salary;

    /*
    fgets(in, 2*sizeof(float), stdin);
    salary = atof(in);

    printf("%f\n", salary);

    printf("%lf\n", 200.00 * 100.00);

    int a = 499999.99;
    float b = 500000.0;
    float c = 500000.1;
    if (a == b)
    {
        printf("yes\n");
        if (b == c)
        {
            printf("also\n");
        }
    }
    if (b == c)
    {
        printf("also\n");
    }
    */
}